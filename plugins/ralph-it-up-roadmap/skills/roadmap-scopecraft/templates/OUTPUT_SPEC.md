# Output Spec (ScopeCraft)

All outputs must:
- be consistent with each other
- reference repo evidence (file paths) where possible
- define “next major stage” with measurable completion criteria
- include acceptance criteria for epics/stories
- include dependencies and risk mitigations